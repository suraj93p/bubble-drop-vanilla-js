// App Constants

export const states = {
  START: 'Start',
  PAUSE: 'Pause',
  FINISH: 'Finish'
};

export const colors = ['red', 'green', 'blue', 'orange', 'yellow'];

export const bubbleClassName = 'bubble';

export const bubbleSizeValues = {
  max: 100,
  min: 10
};

export const animationName = 'slide-bubble';
